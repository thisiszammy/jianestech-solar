{
  "_comment": "Points at JianeTech.Api's dev URL. Correct in both topologies: it is the same origin when the API hosts this app, and the cross-origin API when the client's own dev server serves it.",
  "ApiBaseUrl": "https://localhost:7285/"
}
